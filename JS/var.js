//определение свойств шарика в html
var ball = null;
// счет-звезды в html
var stars = null;
// блок жизней
var lifes = null;
var actualLife = 0;
var newLifes = 3;
// блок стартовой кнопки 
var startBlock = null; // блок с кнопкой
// свойства самой старт-кнопки
var startButton = null// сама кнопка
// обратный отсчет
var seconds = null; // подсчет секунд
// переменная нулевого значения очков
var score = 0;
// создаем переменную для игрового поля
var field = null;
